<nav class="navbar navbar-expand-lg bg-dark mb-5 p-2 sticky-top" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="fs-3 text-center mr-5 text-light text-decoration-none" href="{{path('index')}}">breizh<br/>codeur</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">

      {% block header_navigation_links %}
        <li class="nav-item">
          <a class="nav-link {{ _route =='index' ? 'active' : "" }}" href="{{path('index')}}">
            <i class="bi bi-house-fill fs-3"></i>
            Accueil         
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link {{ _route =='allPosts' ? 'active' : "" }}" href="{{path('allPosts')}}">
            <i class="bi bi-book fs-3"></i>
            Articles 
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link {{ _route =='allTags' ? 'active' : "" }}" href="{{path('allTags')}}">
            <i class="bi bi-tags fs-3"></i>
            Catégories
          </a>
        </li>

      {% endblock %}
      <div class="mt-2">
          <form class="d-flex">
            <input class="form-control me-sm-2" type="search" placeholder="Search">
            <button class="btn btn-info rounded my-2 my-sm-0" type="submit">Search</button>
          </form>
        </div>

        

        {# <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="#">Action</a>
            <a class="dropdown-item" href="#">Another action</a>
            <a class="dropdown-item" href="#">Something else here</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="#">Separated link</a>
          </div>
        </li> #}

      </ul>
      
      <ul class="navbar-nav ml-auto">
    
        {# Si l'utilisateur n'est pas connecté #}
        {% if app.user == false %}        
        <li class="nav-item">
          <a class="nav-link {{ _route =='connexion' ? 'active' : "" }}" href="{{ path('login')}}">
            <i class="bi bi-person-fill fs-3"></i>
            Se connecter
          </a>
        </li>
        {% endif %}

        {# Si l'utilisateur est connecté #}
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link {{ _route =='connexion' ? 'active' : "" }}" href="{{path('user_profile', {username: app.user.username}) }}">
              <i class="bi bi-person-fill fs-3"></i>
              Compte
            </a>
          </li>
            {# Si l'utilisateur est connecté et qu'il est admin#}
              {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item">
                  <a class="nav-link {{ _route =='admin_blog_' ? 'active' : "" }}" href="{{path('admin_blog_index')}}">
                    <i class="bi bi-lock fs-3"></i>
                    Admin         
                  </a>
                </li>
              {% endif %}
            

          <li class="nav-item">
            <a class="nav-link {{ _route =='connexion' ? 'active' : "" }}" href="{{ path('login')}}">
              <i class="bi bi-person fs-3"></i>
              Se déconnecter
            </a>
          </li>
        {% endif %} 
      </ul>
      
      <button 
        class="navbar-toggler collapsed bg-danger" 
        type="button" 
        data-bs-toggle="collapse" 
        data-bs-target="#appNavbar" 
        aria-controls="navbarSupportedContent" 
        aria-expanded="false" 
        aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        

      {# Switch theme #}
      <i class="material-symbols-outlined fs-3">light_mode</i>
        <div class="form-check form-switch">
          <input class="form-check-input" type="checkbox" id="flexSwitchCheckDefault">
          <label class="form-check-label" for="flexSwitchCheckDefault"></label>
        </div>
      <i class="material-symbols-outlined fs-3">dark_mode</i>



    </div>
  </div>
</nav>