{% extends 'base.html.twig' %}

{% block title %}{{post.title}}{% endblock %}

{% block body %}

    <div class="posts_container">

        <div class="post_card">
            <h2 class="card_title">{{post.title}}</h2>
            <div class="card_publication">
                <p>Publié le {{post.publishedAt|date("d/m/Y")}}</p>    
                <p>Par {{post.author.fullname}}</p> 
            </div>
            <p class="card_summary">{{post.content}}</p>
        </div>

    </div>

{% endblock %}
