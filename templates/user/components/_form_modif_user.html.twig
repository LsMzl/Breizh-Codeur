{{ form_start(form, { attr: attr | default({}) }) }}

  {{ form_widget(form) }}

  <button type="submit" class="{{ button_css | default('btn btn-success rounded') }}">
    <i class="bi bi-floppy" aria-hidden="true"></i>
    Valider
  </button>
  <a href="{{ path('user_profile', {id: app.user.id }) }}" class="btn btn-danger rounded">
    <i class="bi bi-columns-reverse" aria-hidden="true"></i>
    Annuler
  </a>

{{ form_end(form) }}